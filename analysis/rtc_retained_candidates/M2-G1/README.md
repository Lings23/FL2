# M2-G1：保留待修正的持续小残差改良方案

登记日期：2026-09-15。用户明确要求保存有明显改善的方案，防止后续直接舍弃。

**状态：有实测效用收益，保留待修正和重新集成；原批次未接受，未晋升主线。**
准确说法不是“平均效果未过验收”：这批所有预登记ACC效用门都通过，失败的是良性误伤门。

## 冻结版本

- 父版本：M2=B0+R1c+R2，原研究别名rtc_i12_combined；配对对照rtc_i12_lower_observe只是增加只读低尾观测。
- 本候选：rtc_i12_lower_cap，相对该对照只将lower_tail_mode由observe改为cap。
- 新机制G1：trainable残差/其他principal残差中位数 < 0.8738694595117251，连续两次参与后q=0。
- 至少9个其他正名义质量principal；参考中位数包括零值且必须>1e-12；正常或无效观测重置连续次数。
- 保留原R1c/R2 cap、cumulative power1、semantic floor .5、MAD k2.5和accepted-anchor回填 .51。
- clean103校准、clean104验证；新闭环实验为IID、seeds201/202的24项。现有证据不是显著性或最终泛化结论。

本目录保存候选与父版本完整resolved配置，以及G1、R1c、R2和基础RTC校准/协议快照。
Linux绝对路径代表原始执行环境，恢复实验须另外准备新合同，不能直接用历史配置在其他环境续训。

## 为什么保留

| LIE .5 指标 | 父版本M2 | M2-G1 |
|---|---:|---:|
| 两seed平均ACC，round11--60 | 77.5934% | 82.7458% |
| 两seed最终ACC | 87.505% | 87.955% |
| seed201每轮恶意权重 | 25.1162% | 3.6000% |
| seed202每轮恶意权重 | 26.8754% | 1.2000% |

平均提升5.1524个百分点、最终提升0.45个百分点；LIE新增良性误标分别1/357和0/353。
各条件完整指标见comparison.csv，逐seed证据与判定见review.json。不能仅报告优势条件而隐去下列失败。

## 为什么尚未接受

24项完整、92项runner质量门通过，38个候选门中35个通过、3个失败：

- Gaussian seed201：候选良性误标8/354=2.2599%，超过1%；已被幅度模块拒绝的异常更新仍污染低尾参考。
- Sign-flip seed201：父版本和候选均4/351=1.1396%；某轮6/10参与者为攻击者，谱参考被其占据。

原protocol、阈值及server decision保持不变。M2历史接受范围不代表其seed201安全转移通过。
本登记保留的是值得修正的研究版本，不是已证明普遍更优或可默认启用的版本。

## 可恢复证据

- candidate.json：稳定编号、机制开关、参数、收益、失败、来源哈希和后续处置要求。
- server_lock.json、review.json：原服务器冻结锁和独立验收快照。
- parent_resolved_seed201.json、candidate_resolved_seed201.json：代表性的完整配对配置。
- audit_bundle.zip：301个审计输入，包含全部JSON/CSV结果、冻结计划、runtime配置和原服务器源码包。
  共116925566字节；SHA256：e709490a3749b16976dd076a4c760aad5b577aa199109d721e8d5d63c4b29766。
  已逐条解压读取并与原文件哈希核对，不包含模型checkpoint和控制台stdout日志。

归档保存在独立分析目录，即使后续更改防御实现，原来源仍可核查。
需要恢复时将大包解压到单独目录，保持内部相对路径；不要覆盖当前实验输出，也不要把提取源码混入活动工作区。
原始绝对路径需使用经过哈希校验的离线导入映射。现有审计入口为analysis/rtc_r3_lower_tail_review/verify.py。
该大包被.gitignore排除，未上传GitHub；单纯git push不会备份它。

## 后续必须闭合的事项

1. 先诊断和单独修复M2参考可靠性，保留原抽样压力与历史判定。
2. 回到G1，修复低尾参考污染并重新校准所需变化；不得通过直接放宽1%误伤上限接受原版。
3. 在修正后的父版本上验证新的低尾组合，同时报告LIE收益保留、Gaussian/Sign-flip/clean回归。
4. 已知seeds201/202属于开发证据；新改法须预登记独立验证，仍需其他攻击、targeted与非IID最终验证。
5. 后续交接明确列出M2-G1的状态与下一动作。若改用其他机制，必须给出替代证据和收益去向，不能静默删除。

本次只做保留登记与证据归档，没有训练、改变验收结论或新增实验批次。
