# M2-H2：误伤修复有效，保留待修正历史写入

研究状态retained_for_memory_repair；本批拒绝、未晋升主线。batch96、IID、seeds201/204，不能与旧batch48结果混算。
seed201 Sign-flip误伤4/351→0/351，最终ACC84.20%→84.52%；其余七个条件/seed配对轨迹不变。
但该seed平均ACC80.1984%→79.9716%（-.2268pp，门为-.2pp），攻击期每轮平均恶意权重
5.3510%→5.6019%（+.2509pp，门为+.1pp）。31门29通过2失败，不能追改历史门宣布接受。

根因：round52四条良性原判断已被H2撤销，却仍写入拒绝历史；round53因此撤销三条正确攻击拒绝。
修复候选H2b只改历史写入：资格门弃权时保持该主体上次状态，当前R2明确拒绝时仍更新为拒绝。
不把弃权当成新拒绝，也不据此清除已有拒绝；正常参与仍按原规则清除。
旧轨迹重放保留四条误伤修复并恢复三条攻击拒绝，不构成闭环ACC或最终泛化证据。
H2b需同时与M2及原H2严格配对，验证误伤收益保留及效用/恶意权重回归，不能静默丢弃本候选。

candidate.json登记完整状态/参数来源；parent/candidate resolved、原protocol、服务器锁和独立review已快照。
audit_bundle.zip保存302份JSON/CSV和冻结产物，包含原服务器源码包；不含checkpoint或stdout日志。
共97097261字节，SHA256 5422938b0157db17f1fe66718ae3066891b7c565b78757d7211bcb8f5cd1e91f。
已逐条读取与原文件哈希核对。大包仅本地保存，被Git忽略，需另行备份；恢复到独立目录，不覆盖活动结果。

G1仍单独保留，其batch48 LIE平均+5.1524pp不等于H2/H2b已集成收益。父版修复后必须返回G1参考修复与组合验收。
